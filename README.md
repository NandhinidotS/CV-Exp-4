# CV-Exp-4
This program uses OpenCV to detect faces in a video and estimate their distance from the camera using a known face width and focal length. It selects the largest detected face, applies smoothing for stability, and displays the distance in centimeters. Optionally, the processed video can be saved.
